// xfail: E269
// test_e269_stray_identifier.bpp — a bare identifier at file scope must be
// REJECTED. Until the 2026-07-07 storage cleanup it silently declared a
// global (the legacy-B `x;` shorthand, semicolon optional!), so a typo'd
// keyword or a leftover word compiled as a variable. Zero uses existed in
// the repo; strays must be heard, not eaten.
contador;

main() {
    return 0;
}
