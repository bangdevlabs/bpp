import "bpp_io.bsm";

struct Vec2 { x, y }

main() {
    var v: Vec2;
    v.x = 42;
    v.y = 100;
    putnum(v.x);
    putline();
    putnum(v.y);
    putline();
    putnum(v.x + v.y);
    putline();
    putmsg("var basic: OK");
    return 0;
}
