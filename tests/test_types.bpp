import "defs.bsm";

auto score;
auto screen_w, screen_h;
byte lives;

main() {
    auto x, y;
    byte hp;
    auto vel;
    x = 400;
    y = 300;
    hp = 100;
    vel = 5;
    lives = 3;
    score = 0;
    screen_w = 800;
    screen_h = 600;
    return 0;
}
