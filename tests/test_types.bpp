import "bootstrap/bpp_defs.bpp";

auto score;
word screen_w, screen_h;
byte lives;

main() {
    word x, y;
    byte hp;
    auto vel;
    x = 400;
    y = 300;
    hp = 100;
    vel = 5;
    lives = 3;
    score = 0;
    screen_w = 800;
    screen_h = 600;
    return 0;
}
