// xfail: E246
// V3 Session 3 negative gate. Annotation contract says float, caller
// passes a word — E246 hard error.
import "bpp_io.bsm";

void cb_float(x: float) { putmsg("never"); }

main() {
    auto fp: func(float) -> void;
    fp = fn_ptr(cb_float);
    auto i; i = 16;
    call(fp, i);
    return 0;
}
