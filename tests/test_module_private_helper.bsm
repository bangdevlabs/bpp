// Helper for test_module_private_globals — a `static` global with the SAME
// name as main's, in a DIFFERENT module. Module-private: its own slot.
static auto _shared_scratch;

mpg_helper_set(v) { _shared_scratch = v; }
mpg_helper_get()  { return _shared_scratch; }
