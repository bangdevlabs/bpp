// xfail-warn: W028
// V3 Session 2 negative gate. Local origin resolves but the arg type
// does not match the callee's declared parameter — W028 must fire.
// Compile must succeed (warnings are non-blocking); runner asserts on
// the W028 line in stderr.
import "bpp_io.bsm";

void cb_float(x: float) { putmsg("hi"); putline(); }

main() {
    auto i; i = 16;            // int
    auto fp;
    fp = fn_ptr(cb_float);
    call(fp, i);                // W028: int arg → float param
    return 0;
}
