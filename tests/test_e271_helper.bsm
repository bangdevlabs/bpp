// Helper for test_e271_static_global_collision — declares a static global
// with the SAME name, in a DIFFERENT module. Under the module-private
// contract these must not share a slot; today they would, so it is E271.
static auto _shared_scratch;

e271_touch() {
    _shared_scratch = 2;
    return _shared_scratch;
}
