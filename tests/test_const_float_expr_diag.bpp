// xfail: E267
// test_const_float_expr_diag.bpp — the const evaluator must REFUSE float
// shapes it cannot fold (know-or-refuse doctrine), never chew the decimal's
// bytes into silent integer garbage. A single float literal — positive or
// negated — takes the dedicated fast paths and works (test_const_float);
// float ARITHMETIC in a const right-hand side has no integer answer, so it
// must fail the compile with E267 pointing at the workaround.
const BAD = 2.5 * 2.0;
main() {
    return BAD;
}
