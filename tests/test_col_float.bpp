import "stbcol.bsm";
import "bpp_io.bsm";

main() {
    auto r;
    r = rect_overlap(10.0, 10.0, 16.0, 16.0, 20.0, 20.0, 16.0, 16.0);
    if (r) { print_msg("HIT"); }
    else { print_msg("MISS"); return 1; }
    return 0;
}
