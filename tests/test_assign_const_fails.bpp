// xfail: E263
// test_assign_const_fails.bpp — confirm the validate pass rejects
// writes to a `global const` slot. The storage-class sidequest
// (2026-05-14) ships these slots as compiler-enforced read-only
// after init; assignment is a contract violation.

global const X = 7;

main() {
    X = 99;
    return 0;
}
