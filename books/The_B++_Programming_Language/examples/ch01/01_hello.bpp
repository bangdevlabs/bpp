// The smallest complete B++ program. It prints one line and stops.
//
// `main` is where every program starts. `put` is the friendly output
// helper: handed a string, it writes the text as-is. When `main`
// reaches its closing brace it returns 0 (success) automatically.
main() {
    put("Hello, B++\n");
}
